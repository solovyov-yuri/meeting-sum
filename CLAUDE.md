# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The full, canonical agent contract lives in `AGENTS.md` and is imported below, so the two files can
never drift. Edit `AGENTS.md`, not this file, for shared rules; keep only Claude-Code-specific notes here.

@AGENTS.md

## Claude-Code-specific notes

- On first load, Claude Code will ask once to approve the `@AGENTS.md` import — approve it so the shared
  rules load.
- There are currently no Claude-only rules beyond the shared contract above. Add any here rather than
  duplicating `AGENTS.md`.
