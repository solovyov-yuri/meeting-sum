# Ревью 2026-07-02 (первый запуск)

## Метод

Четыре параллельных read-only агента: (1) ARCH+CODE — src/, tests/, desktop/ против спек;
(2) REL+SEC+PERF — включая проверку git-трекинга секретов и данных; (3) DEP+CONV+DOC — с реальным
запуском ruff/mypy/pytest; (4) AGENT — конфигурация + майнинг истории сессий
(`/home/samogonn/.claude/projects/-mnt-c-Users-solov-projects-job-inno-recap/`). Ключевые находки
верифицированы напрямую по исходникам основным агентом. Пересечения между агентами дедуплицированы
(cancel-механика, stderr→null, устаревший CLAUDE.md, мёртвый output_format — каждую нашли
независимо по 2–3 агента, что повышает уверенность).

## Итоги запуска

- **Новых issue: 40** (все — это первый запуск). critical: 0, high: 8, medium: 17, low: 15.
- Повторно наблюдаемых: — (нет прежних).
- Не наблюдаемых из прежних: — (нет прежних).
- Из находок агентов отброшено как недоказуемое: «брифы сессий дублируют правила CLAUDE.md»
  (n=1 содержательная сессия — паттерн не установить; отражено в профиле ниже, не как issue).

## Новые ID

ARCH-001…004; CODE-001…007; DEP-001…004; REL-001…008; SEC-001…004; PERF-001…002; CONV-001;
DOC-001…002; AGENT-001…008.

Гипотезы (требуют подтверждения): SEC-004 (fallback SDK на OPENAI_API_KEY), REL-003 (мигающая
консоль в release-сборке), REL-007 (SSE-ошибки мимо retry), macOS-часть DEP-003.

## Состояние инструментальных проверок (наблюдалось в этом запуске)

- `ruff check src/` — All checks passed
- `mypy src/` — Success: no issues found in 15 source files
- `pytest -q` — 298 passed

## Верифицированно чистое (проверялось, находок нет)

Атомарные записи повсюду; frozen-датаклассы; ленивые импорты провайдеров; фабрика — единственная
точка сборки; `config.yaml.example` совпадает со схемой; `config.yaml` и записи в `data/` не
закоммичены (история git проверена); ffmpeg без shell=True; yaml.safe_load/safe_dump; ключи —
только в keychain, наружу — только маскированный boolean; Tauri capabilities минимальны;
integration-тесты мокают только на границе фабрики.

## Профиль workflow (для AGENT-категории)

Пользователь пишет по-русски; код/доки — английские, UI продукта — русский. Паттерн работы:
крупные автономные фичи по заранее подготовленным спекам (desktop MVP: одна сессия ~3.8 МБ,
~5–6 реплик пользователя), затем циклы ревью-и-фикс с точными file:line баг-репортами. История
тонкая (два содержательных дня: 19.06, 02.07) — выводов о «повторяющихся коррекциях» сделать
нельзя; единственный зафиксированный инцидент доверия — ложный отчёт о проверках (AGENT-006).
Среда: Claude Code в WSL против Windows-чекаута с Windows-venv; tauri/cargo — только вручную на
стороне Windows (AGENT-007); двойной `.claude`-home — корень AGENT-001/AGENT-004.

## Примечание безопасности

В `config.yaml:39` обнаружен живой API-ключ (SEC-001). В отчёте значение не воспроизводится;
ключ читали процессы этого ревью — рекомендована ротация.
