# Ревью 2026-07-05 (второй запуск)

## Метод

Повторный запуск через три дня после первого (2026-07-02), все 40 прежних issue к этому моменту
закрыты (`done`) burndown-ом 02.07. Фокус — код, добавленный после первого ревью (~15 коммитов):
структурный пайплайн саммари (`models`/`summary_schema`/`formatters`), portable-сборка с
загрузкой CUDA on-demand (`cuda_support.py`, `packaging/`, `scripts/`), загрузка Ollama-моделей
(`ollama_support.py`), persistent worker, режим `lecture` — плюс регрессионная проверка прежних
фиксов. Четыре параллельных read-only агента: (1) ARCH+CODE, (2) REL+SEC+PERF (сетевые
поверхности), (3) DEP+CONV+DOC с реальным прогоном всех проверок, (4) AGENT (конфигурация +
майнинг истории сессий после 02.07). Ключевые находки верифицированы основным агентом напрямую:
round-trip-инвариант — исполняемым репро на venv-питоне (ROUNDTRIP BROKEN), регрессия
duration-probe — по git-истории, отмена resummarize / целостность CUDA-загрузки /
save_summary-пути — чтением исходников. Пересечения агентов дедуплицированы (контракт моста,
timeout Ollama-pull, CUDA-пины, write-скоупинг — каждое нашли независимо по 2 агента).

## Итоги запуска

- **Новых issue: 32.** critical: 0, high: 2, medium: 13, low: 17.
- **Повторно наблюдаемых (прежние, ещё открытые): 0** — все 40 прежних были `done`.
- **Не наблюдаемых из прежних: —** (все закрыты; повторных проявлений закрытых проблем в прежнем
  виде не обнаружено).
- **Регрессия прежней работы: 1** — CODE-008: коммит `da9d474` откатил duration-probe из
  `3dd5381`, прогресс транскрибации снова молчит при `info.duration=0` (случай реального входа
  пользователя).
- **Рецидивы паттернов** (закрытых issue не переоткрывают, заведены новые): DOC-003 ≈ DOC-001
  (контракт моста снова отстал), CONV-002 ≈ CONV-001 (first-party-список снова дрейфует),
  AGENT-009 ≈ AGENT-002 (AGENTS.md снова материально ложен — теперь активно запрещает
  существующую унификацию CLI). Системная причина зафиксирована в AGENT-009: обновление
  доков/контрактов не входит в definition-of-done изменения.
- Гипотезы (требуют подтверждения на живой машине): REL-014 (ротация лога при живом worker),
  CODE-012 (раскраска шагов истории), auto-часть REL-010, AGENT-015 (паттерны allowlist).

## Новые ID

ARCH-005…006; CODE-008…012; DEP-005…006; REL-009…014; SEC-005…009; PERF-003; CONV-002;
DOC-003…005; AGENT-009…015.

## Состояние инструментальных проверок (наблюдалось в этом запуске)

- `pytest.exe -q` — **1 failed**, 349 passed (падение — SEC-005: неизолированный `RECAP_*`-env;
  в diff падения печатается живой API-ключ)
- `ruff.exe check src/` — All checks passed
- `mypy.exe src/` — Success, 18 файлов
- `npm run lint` — 0 ошибок, 2 старых warning (`react-refresh/only-export-components`)
- `npm run test -- --run` — 9 passed; `npm run build` — OK
- `cargo.exe check` / `clippy` — чисто, 0 warnings
- `npm audit --audit-level=high` — 0 уязвимостей

## Верифицированно чистое (проверялось, находок нет)

`to_html` экранирует весь LLM-текст (XSS в экспорте нет); JSON→text фолбэк `_generate_summary`
корректен; секреты не логируются нигде в новом коде (`_settings_to_dict` — только маска);
history/settings — атомарные записи под межпроцессным локом; жизненный цикл persistent worker
(EOF stdin → самозавершение, kill на Exit, деградация в spawn-per-call); cancel-флаг per-run без
гонок; дизайн «сентинел последним» в CUDA-кэше корректно переживает kill/cancel/переполнение
диска; git-артефакты не закоммичены; ленивые импорты и слоистость новых модулей (pure stdlib);
фабрика — единственная точка сборки; `config.yaml.example` синхронен со схемой; тесты на все
четыре новых нетривиальных модуля есть; `AUDIO_EXTENSIONS` зеркалится в bridge.ts;
`CLAUDE.md → @AGENTS.md` работает; deny `uv` на месте; нарушений AGENTS.md агентами после 02.07
не обнаружено (uv не вызывался, git — только по запросу, ложных заявлений о верификации нет).

## Профиль workflow (для AGENT-категории)

После 02.07 — четыре содержательные сессии: короткая проверка `@AGENTS.md`-импорта; burndown всех
40 issue (7.8 МБ, перетёк в цикл UX-полировки); отдельная сессия по багу прогресса, открытая
**агентски сгенерированным handoff-промптом** (баг решён с первого захода через лог моста);
сессия structured-пайплайна + portable + загрузок CUDA/Ollama. Язык — русский; коммиты —
английские Conventional Commits через `/commit`. Характерный цикл: пользователь вручную гоняет
app на Windows и возвращает нумерованные пачки замечаний с file:line; агент чинит. Главное трение
периода — слепая зона Windows-рантайма (3+ итераций на баге прогресса, пока диагностика не
переключилась на лог моста) → находки AGENT-011/013. Удачные практики: handoff-промпт (кандидат в
повторяемую практику), честные «UNVERIFIED scaffolding» в сообщениях коммитов.

## Примечание безопасности

Живой ключ `RECAP_SUMMARIZATION_MODEL_API_KEY` из пользовательского окружения печатается в
вывод pytest (SEC-005) и был прочитан процессами этого ревью; значение в отчёте не
воспроизводится, рекомендована ротация.
